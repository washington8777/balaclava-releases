# Balaclava Releases

Update feed + release archives for the Balaclava app. The appcast at
`appcast.xml` is consumed by the in-app Sparkle updater — install once,
updates arrive automatically.

Source lives in a separate private repository.
